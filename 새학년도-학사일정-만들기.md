# 새 학년도 학사일정 시트 만들기

매년 초, 학사일정 원본 스프레드시트를 복사해서 다음 학년도용 뼈대(날짜 칸)를
자동으로 만들어 주는 절차입니다. `Code.gs`의 `createSchoolYearCalendar` 함수를
실행하면 됩니다.

## 1. Apps Script 에디터에서 실행하기

1. 학사일정 원본 스프레드시트(또는 이 코드가 들어있는 Apps Script 프로젝트)를
   열고 **확장 프로그램 → Apps Script**로 에디터를 엽니다.
2. `Code.gs`에 이 레포의 최신 내용을 붙여넣고 저장합니다 (`Ctrl+S`).
3. 상단 함수 선택 드롭다운에서 `createSchoolYearCalendar`를 선택합니다.
4. **실행** 버튼을 누르기 전에, 함수 맨 위에 임시로 아래처럼 한 줄 추가해서
   원하는 학년도를 넣어주세요 (예: 2027학년도용을 만들 때):

   ```js
   function createSchoolYearCalendar(schoolYear) {
     schoolYear = 2027; // ← 실행 전 이 줄을 넣고, 끝나면 지워도 됩니다
     ...
   ```

   (Apps Script 에디터에서 "실행"은 인자를 직접 넘길 수 없어서, 이렇게 임시로
   값을 박아넣고 실행한 뒤 지우는 방식이 가장 간단합니다.)
5. 실행합니다. **처음 실행하면 권한 승인 화면**이 뜹니다 — 이 함수는
   Google Drive(스프레드시트 복사)와 Google Calendar(공휴일 조회) 권한이
   필요하므로, 두 권한을 모두 허용해 주세요.

## 2. 실행 로그 확인하기

실행이 끝나면 **보기 → 실행 로그** (또는 `Ctrl+Enter`)에서 다음을 확인합니다.

- 새로 만들어진 스프레드시트 주소(URL)
- 각 탭(1학기 종합/2학기 종합)에 며칠치 날짜를 채웠는지, 공휴일 몇 건을
  자동으로 넣었는지
- 관리자 화면에 붙여넣을 JSON (아래 3번 참고)

## 3. 새 시트에서 손으로 채워야 하는 것

자동으로 채운 것은 **날짜 칸(A열)과 토/일 배경색, 공휴일(D열) 뿐**입니다.
아래는 학교 사정에 따라 매년 달라지는 값이라 일부러 비워뒀습니다 — 반드시
손으로 채워주세요.

1. **B열(행)** — 순환 시간표 코드 (`P/T/X/AB/AF` 등)
2. **C열(열)** — 누적 수업일수
3. **방학 기간** — 여름/겨울방학 시작·종료일, 개학식/방학식 등은
   `학사일정표`, `달력` 탭과 맞춰서 직접 조정
4. **D~M열의 실제 학사일정 내용** — 전교사일정/학생일정/기타 일정, 광장·세미나실·
   댄스실·체육관 예약 등 (공휴일 이외의 학사일정은 자동으로 채워지지 않습니다)

## 4. 관리자 화면에 등록하기

실행 로그 맨 아래에 아래와 같은 JSON이 출력됩니다.

```json
{"spreadsheetId":"<새 스프레드시트 ID>","tabs":[{"id":"<gid>","name":"<탭 이름>"}, ...]}
```

이 JSON을 통째로 복사해서 **광수GPT 관리자 화면 → "학사일정 원본" 설정**에
붙여넣으면 연도 전환이 끝납니다.

> 참고: 이 관리자 화면("학사일정 원본" 설정 UI)은 이 작업과 별개로 3세대
> 광수 Online 교무실 쪽에서 따로 구현 중입니다. 아직 화면이 없다면, 위 JSON을
> 담당자에게 전달해 두면 됩니다.
